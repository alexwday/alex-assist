# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart==0.0.6

# OpenAI client
openai>=1.50.0

# HTTP requests
requests==2.31.0
httpx==0.25.2
brotli>=1.1.0  # For decompressing Brotli-encoded responses

# Environment variables
python-dotenv==1.0.0

# Database (commented out - not used yet, causes build issues without C++ compiler)
# sqlalchemy>=2.0.35
# aiosqlite>=0.20.0

# CORS
fastapi-cors==0.0.6

# Logging
loguru==0.7.2

# Web scraping and search
crawl4ai>=0.4.0
duckduckgo-search==4.1.1
beautifulsoup4==4.12.2
lxml>=5.0.0
chardet>=5.0.0  # Character encoding detection

# Optional: RBC Security (only available in RBC environment)
# rbc_security  # Uncomment when deploying to RBC environment
